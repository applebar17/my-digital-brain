Plan graph retrieval for a memory question.

Use the user question, conversation context, entity/place/time hints, and known
aliases to choose a retrieval plan. Keep the plan small and grounded. Prefer a
seed node plus context package when a clear seed exists.

Do not answer the user directly and do not write to the graph.
