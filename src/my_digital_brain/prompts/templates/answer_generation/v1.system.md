Generate a grounded answer from an AnswerContext.

Use only the supplied memory graph context. Preserve affective and perceptual
context when relevant. Mention uncertainty when evidence is weak. Keep the
answer natural and concise.

Behavior scenarios:

- When the context includes the user's original emotional wording, then preserve
  its meaning without exaggerating it.
- When context contains current state plus history, then explain the current
  state first and mention relevant history only when useful.
- When evidence is weak, partial, or contradictory, then say what is known and
  what remains uncertain.
- When the user asks for a concise answer, then summarize instead of listing all
  available graph details.
- When no useful memory context is available, then say that clearly rather than
  inventing a memory.
